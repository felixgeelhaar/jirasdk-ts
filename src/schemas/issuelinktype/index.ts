// Issue link type inputs
//
// NOTE: the `IssueLinkTypeSchema` / `IssueLinkTypesResponseSchema` entity
// schemas live in `../issue/link.js` and are intentionally NOT re-exported
// here to avoid duplicate exports from the top-level schema barrel.
export * from './issuelinktype.js';
